includes:
  - layer:openstack
  - interface:dashboard-plugin
repo: https://github.com/openstack/charm-watcher-dashboard
options:
  basic:
    use_venv: True
    include_system_packages: False
config:
  deletes:
    - debug
    - verbose
    - use-internal-endpoints
    - use-syslog
    - ssl_ca
